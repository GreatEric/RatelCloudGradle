server:
  #address: 0.0.0.0
  port: 19081

spring:
  main:
    allowBeanDefinitionOverriding: true
  application:
    name: ratel-composition-ecommerce
  cloud:
    nacos:
      config:
        server-addr: ${cloud.nacos.server-addr}
        file-extension: properties
      discovery:
        server-addr: ${cloud.nacos.server-addr}