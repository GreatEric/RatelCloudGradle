server:
  #address: 0.0.0.0
  port: 17081

spring:
  main:
    allowBeanDefinitionOverriding: true
  application:
    name: ratel-service-system-gateway
  cloud:
    nacos:
      config:
        server-addr: ${cloud.nacos.server-addr}
        file-extension: properties
      discovery:
        server-addr: ${cloud.nacos.server-addr}