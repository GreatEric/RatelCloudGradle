server:
  #address: 0.0.0.0
  port: 18083

spring:
  main:
    allowBeanDefinitionOverriding: true
  application:
    name: ratel-service-product
  cloud:
    nacos:
      config:
        server-addr: ${cloud.nacos.server-addr}
        file-extension: properties
      discovery:
        server-addr: ${cloud.nacos.server-addr}